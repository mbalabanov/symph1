
{% extends 'base.html.twig' %}
{% block body %}

    {% for message in app.flashes('notice') %}
        <div class="alert alert-success mt-4">
            {{ message }}
        </div>
    {% endfor %}

    <h2  class="mt-5">Add Journey</h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}

{% endblock %}